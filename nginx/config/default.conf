#server {
#    listen 8080;
#    server_name *.colinbruner.com;
#    access_log /dev/stdout;
#    error_log /dev/stdout info;
#
#    keepalive_timeout 70;
#    gzip on;
#    gzip_disable "msie6";
#    gzip_vary on;
#    gzip_proxied any;
#    gzip_comp_level 6;
#    gzip_buffers 16 8k;
#    gzip_http_version 1.1;
#    gzip_types text/plain text/css application/json application/x-javascript application/javascript text/xml application/xml application/xml+rss text/javascript;
#
#    location / {
#        proxy_pass "http://s3.colinbruner.com.s3-website.us-east-2.amazonaws.com";
#    }
#}

server {
    listen 8080;
    server_name *.colinbruner.com;
    access_log /dev/stdout;
    error_log /dev/stdout info;

    keepalive_timeout 70;
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_types text/plain text/css application/json application/x-javascript application/javascript text/xml application/xml application/xml+rss text/javascript;

    location / {
        resolver 1.1.1.1;
        set $bucket "s3.colinbruner.com.s3-website.us-east-2.amazonaws.com";
        rewrite ^([^.]*[^/])$ $1/ permanent;

        # matches: branch.domain.com
        if ($host ~ ^([^.]*)\.colinbruner\.com) {
            set $branch $1;
            proxy_pass http://$bucket/${branch}${uri};
        }

        proxy_pass http://$bucket;

        proxy_intercept_errors on;
        proxy_redirect off;
        proxy_set_header Host $bucket;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_hide_header x-amz-id-2;
        proxy_hide_header x-amz-request-id;
    }
}
